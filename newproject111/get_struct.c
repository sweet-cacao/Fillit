#include "fillit.h"

void    get_struct(t_term **term, char *buff)
{
    int i;

    i = 0;
    if (!term)
        term = (t_term *)memalloc(sizeof(t_term *));
    while (buff[i])
    {
        if (buff[i] == "")
    }
}

