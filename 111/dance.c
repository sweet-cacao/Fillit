#include "fillit.h"

void    map_create(t_tetrim *tetr)
{
    t_row *row;
    
    add_row()
}

void    solve_board(map)
{
    if (size == 0)
        return (map);
    else
    {
        while ()
    }
    
}

void    solver(t_map *map)
{
    while (!(solve_board))
    {
        create_map;
        solver(map);
    }

}